import styled from 'styled-components';

export const Container = styled.footer`
  height: 5rem;

  display: flex;
  align-items: center;
  justify-content: center;

  p {
    font-size: 16px;
    color: var(--text);

    span {
      font-weight: bold;
    }
  }
`;
